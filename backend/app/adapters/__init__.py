"""Optional real-data adapters.

Adapters in this package never import optional vendor packages at module import time.
They report an unavailable status when dependencies or credentials are absent.
"""
